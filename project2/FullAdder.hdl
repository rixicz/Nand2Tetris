CHIP FullAdder {
    IN a, b, c;
    OUT sum, carry;

    PARTS:
    // Sum Logic
    Xor(a=a, b=b, out=xorab);
    Xor(a=xorab, b=c, out=sum);

    // Optimized Carry Logic
    And(a=a, b=b, out=andab);
    And(a=xorab, b=c, out=andxabc);
    Or(a=andab, b=andxabc, out=carry);
}